language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install -g hexo-cli
  - npm install

#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "lijiangwei"
  - git config user.email "544172066@qq.com"
  - git add .
  - git commit -m "Update blog"
  # 推送到github
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
  # 推送到gitee
  - git push --force --quiet "https://${GE_REF}=${GE_TOKEN}" master:master
# E: Build LifeCycle

branches:
  only:
    - master
env:
 global:
   - GH_REF: github.com/lijiangwei/lijiangwei.github.io.git
   - GE_REF: gitee.com/lijiangwei/lijiangwei.git?private_token