language: node_js
node_js: stable

cache:
    apt: true
    directories:
        # 缓存不经常更改的内容
        - node_modules

# S: Build Lifecycle
install:
  - npm install -g hexo-cli
  - npm install

#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "lijiangwei"
  - git config user.email "544172066@qq.com"
  - git add .
  - git commit -m "Update Blog By TravisCI With Build $TRAVIS_BUILD_NUMBER"
  # 推送到github
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
  # 推送到gitee
  #- git push --force --quiet "https://lijiangwei:${GE_TOKEN}@${GE_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - master
env:
 global:
   - GH_REF: github.com/lijiangwei/lijiangwei.github.io.git
   #- GE_REF: gitee.com/lijiangwei/lijiangwei.git